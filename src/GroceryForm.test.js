import { shallow } from "enzyme"
import Grocery from './Grocery'
import GroceryForm from './GroceryForm'

describe('App', () => {
  it('should add a grocery to the list', () => {
    // const wrapper = shallow(<GroceryForm />)

  })
})